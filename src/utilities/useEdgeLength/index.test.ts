import { describe, expect, it } from 'vitest'
import {useEdgeLength} from "./index";

describe('useEdgeLength', () => {
  it('should be defined', () => {
    expect(useEdgeLength).toBeDefined()
  })
})
